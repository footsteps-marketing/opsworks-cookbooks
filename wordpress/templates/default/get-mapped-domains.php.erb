<?php 
$_SERVER['HTTP_HOST'] = 'fsm-wp.env';

if ( !isset($wp_did_header) ) {

    $wp_did_header = true;

    require_once( dirname(__FILE__) . '/wp-load.php' );
}

$rows = $wpdb->get_results( "SELECT * FROM {$wpdb->dmtable} ORDER BY id DESC LIMIT 0,20" );
foreach ($rows as $row) {
    echo "{$row->domain}\n";
}

die();
